#!/bin/sh

pid=`pgrep -f "{{ amq_router_start_command }}"`

if [ ! -z "$pid" ]; then
  echo "Stopping router [{{ router_id }}] daemon process (pid=$pid)..."
  kill -9 $pid
else
  echo "Router [{{ router_id }}] is not running"
fi
