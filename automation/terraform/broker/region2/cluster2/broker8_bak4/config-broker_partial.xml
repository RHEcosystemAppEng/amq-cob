        <!-- For setting up mirroring between brokers -->
        <broker-connections>
            <!-- Mirror to Dallas Backup server and Live as the backup (of backup) -->
            <amqp-connection uri="tcp://10.71.128.62:5672#tcp://10.71.0.61:5672" name="Tor_Backup_Live_04">
                <mirror/>
            </amqp-connection>
        </broker-connections>

        <security-enabled>false</security-enabled>

        <ha-policy>
            <shared-store>
                <slave>
                    <failover-on-shutdown>true</failover-on-shutdown>
                    <allow-failback>true</allow-failback>
                    <restart-backup>true</restart-backup>
                </slave>
            </shared-store>
        </ha-policy>

        <connectors>
            <!-- Connector used to be announced through cluster connections and notifications -->
            <connector name="artemis">tcp://10.76.0.62:61616</connector>
        </connectors>

        <acceptors>
            <!-- Acceptor for every supported protocol -->
            <acceptor name="artemis">tcp://10.76.0.62:61616?tcpSendBufferSize=1048576;tcpReceiveBufferSize=1048576;amqpMinLargeMessageSize=102400;protocols=CORE,AMQP,STOMP,HORNETQ,MQTT,OPENWIRE;useEpoll=true;amqpCredits=1000;amqpLowCredits=300;amqpDuplicateDetection=true;supportAdvisory=false;suppressInternalManagementObjects=false</acceptor>

            <!-- AMQP Acceptor.  Listens on default AMQP port for AMQP traffic.-->
            <acceptor name="amqp">tcp://10.76.0.62:5672?tcpSendBufferSize=1048576;tcpReceiveBufferSize=1048576;protocols=AMQP;useEpoll=true;amqpCredits=1000;amqpLowCredits=300;amqpMinLargeMessageSize=102400;amqpDuplicateDetection=true</acceptor>
        </acceptors>

        <cluster-user>amq_cluster_user</cluster-user>
        <cluster-password>amq_cluster_password</cluster-password>

        <broadcast-groups>
            <broadcast-group name="bg-group1">
                <broadcast-period>5000</broadcast-period>
                <jgroups-file>jgroups-ping.xml</jgroups-file>
                <jgroups-channel>active_broadcast_channel</jgroups-channel>
                <connector-ref>artemis</connector-ref>
            </broadcast-group>
        </broadcast-groups>

        <discovery-groups>
            <discovery-group name="dg-group1">
                <jgroups-file>jgroups-ping.xml</jgroups-file>
                <jgroups-channel>active_broadcast_channel</jgroups-channel>
                <refresh-timeout>10000</refresh-timeout>
            </discovery-group>
        </discovery-groups>

        <cluster-connections>
            <cluster-connection name="my-cluster">
                <connector-ref>artemis</connector-ref>
                <retry-interval>500</retry-interval>
                <use-duplicate-detection>true</use-duplicate-detection>
                <message-load-balancing>STRICT</message-load-balancing>
                <max-hops>1</max-hops>
                <discovery-group-ref discovery-group-name="dg-group1"/>
            </cluster-connection>
        </cluster-connections>